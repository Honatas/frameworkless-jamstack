<div class="p-3 flex">
  <div>
    Search filter:
    <select id="filter-options" class="form-field">
      {{#each options}}
        <option value="{{ value }}">{{ label }}</option>
      {{/each}}
    </select>
  </div>

  <div>
    <input type="text" id="filter-input" class="form-field"/>
  </div>

  <div>
    <button type="button" id="bt-search" class="form-button">Search</button>
  </div>
</div>
